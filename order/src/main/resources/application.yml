server:
  port: 8081
spring:
  application:
    name: order
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
  config:
    import:
      - optional:nacos:${spring.application.name}.yaml

# Feign 接入 Sentinel（与 Nacos 中 cloud.sentinel 配置配合使用）
feign:
  sentinel:
    enabled: true